import React, {useState, useEffect} from 'react';
import {View, Text} from 'react-native';

const LoginMeteo = ({navigation}) => {
  // creation de la fonction qui vas envoyer les donnees a l'API
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

export default LoginMeteo;
